import './app.js'; const save = (id, v) => localStorage.setItem(id, v); document.querySelectorAll('input,select').forEach(el => { const key = 'nebula_' + el.id; const value = localStorage.getItem(key); if (value !== null) { if (el.type === 'checkbox') el.checked = value === '1'; else el.value = value } }); document.querySelectorAll('input,select').forEach(el => el.onchange = () => save('nebula_' + el.id, el.type === 'checkbox' ? (el.checked ? '1' : '0') : el.value));
